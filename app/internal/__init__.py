'''
There's a subdirectory app/internal/ with another file __init__.py,
so it's a "Python subpackage": app.internal.

The file app/internal/admin.py is inside a package, app/internal/, so,
it's a submodule: app.internal.admin.
'''